# {{ROOT_DIR}}

Replace this with what every session in this project should know: what the
project is, how it is laid out, which tool owns which concern. It is injected on
every session start and every subagent, so keep it short.

Detail lives in cards, loaded when a matching file is touched. To read one on
demand: `{{CONTEXT_DIR}}/cards/<key>.md`.
